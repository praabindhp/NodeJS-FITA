// Import the entire module using `require` function

const math = require('./1-Modules');

const result = math.add(5, 3);
const result2 = math.subtract(5, 3);
console.log(result);
console.log(result2);

// Output: 8
